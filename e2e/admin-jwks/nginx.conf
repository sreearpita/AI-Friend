server {
    listen 80;
    server_name _;

    location = /jwks.json {
        default_type application/json;
        add_header Cache-Control "no-store";
        root /usr/share/nginx/html;
    }
}
